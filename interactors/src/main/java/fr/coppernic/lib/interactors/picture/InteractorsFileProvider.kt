package fr.coppernic.lib.interactors.picture

import androidx.core.content.FileProvider

/**
 * Custom file provider of this lib to not collide with other file's providers
 */
class InteractorsFileProvider : FileProvider()
